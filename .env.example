# Токен бота от BotFather
BOT_TOKEN=111111111:ABCDEFG

# Тип базы данных: mysql или postgres
DB_TYPE=mysql

# Параметры подключения к базе данных
DB_USER=12341234
DB_PASSWORD=12341234
DB_NAME=defender_test

# Для TCP-подключения (если не указан DB_SOCKET)
DB_HOST=localhost
# Опционально: 3306 для MySQL, 5432 для PostgreSQL
DB_PORT=3306

# Для Unix-сокета (только для MySQL, если не задан DB_PORT)
#DB_SOCKET=/var/run/mysqld/mysqld.sock

# ID чата, где бот будет работать (например, -1001234567890 для групп)
ALLOWED_CHAT_ID=-13131231313123

# Токен бота от BotFather
BOT_TOKEN=111111111:ABCDEFG

# Тип базы данных: mysql или postgres
DB_TYPE=mysql

# Параметры подключения к базе данных
DB_USER=12341234
DB_PASSWORD=12341234
DB_NAME=defender_test

# Для TCP-подключения (если не указан DB_SOCKET)
DB_HOST=localhost
# Опционально: 3306 для MySQL, 5432 для PostgreSQL
DB_PORT=3306

# Для Unix-сокета (только для MySQL, если не задан DB_PORT)
#DB_SOCKET=/var/run/mysqld/mysqld.sock

# ID чата, где бот будет работать (например, -1001234567890 для групп)
ALLOWED_CHAT_ID=-13131231313123

# Таймеры (в секундах)
LANGUAGE_SELECTION_TIMEOUT=300     # Таймаут выбора языка (5 минут)
QUIZ_ANSWER_TIMEOUT=30             # Таймаут ответа на квиз (30 секунд)
MESSAGE_DELETE_DELAY_CORRECT=10    # Задержка удаления сообщения при правильном ответе (10 секунд)
MESSAGE_DELETE_DELAY_INCORRECT=30  # Задержка удаления сообщения при неправильном ответе (30 секунд)
MESSAGE_DELETE_DELAY_TIMEOUT=60    # Задержка удаления сообщения при таймауте квиза (60 секунд)
DEFAULT_MESSAGE_DELETE_DELAY=5     # Задержка удаления сообщений по умолчанию (5 секунд)
MUTE_DURATION=86400                # Длительность мута (24 часа)
UNBAN_DELAY=2                      # Пауза перед анбаном (2 секунды)
DB_DELETE_DELAY=5                  # Пауза перед удалением из БД (5 секунд)
CLEANUP_INTERVAL=120               # Интервал проверки истекших банов (2 минуты)